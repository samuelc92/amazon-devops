apiVersion: v1
kind: Service
metadata:
  name: {{ include "t.fullname" . }}
  labels:
    {{- include "t.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t.selectorLabels" . | nindent 4 }}
